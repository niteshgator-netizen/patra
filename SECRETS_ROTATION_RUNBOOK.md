# SECRETS ROTATION RUNBOOK
Generated by launch-night backend pass (TAB B). **Zero secret values appear in this file.**

## WHY ROTATION IS MANDATORY
Git history is permanent: removing a file from the repo does NOT remove it from history,
and this repo is pushed to GitHub remotes (`clean`, `origin`). Anything that was ever
committed must be treated as exposed and rotated. Untracking + .gitignore (done tonight)
only prevents FUTURE leaks.

## CONFIRMED IN GIT HISTORY — ROTATE THESE FIRST (verified tonight)
| Secret | Where it lived | Rotate on | Then set Render env var |
|---|---|---|---|
| Telegram bot token | `patra-automation-BACKUP/milky_way/README.md` (tracked until tonight; literal token in history, e.g. commit 0d4c177) | Telegram @BotFather → `/revoke` then `/token` for the bot | `TELEGRAM_BOT_TOKEN` (Web Service + Background Worker) |
| Juwa panel secret key | Hardcoded fallback in `config/initializers/juwa_credentials.rb` + `app/services/games/juwa/client.rb` (removed tonight, still in history) | Juwa agent panel (ht.juwa777.com) — request new secret key from the panel/upline | `JUWA_SECRET_KEY` |
| Juwa agent ID | Same two files (identifier, not a secret — exposure is low risk but it pairs with the key) | n/a (rotating the secret key is what matters) | `JUWA_AGENT_ID` |

Notes:
- `patra-automation-BACKUP/` is now untracked + gitignored; files remain on disk only.
- The live `patra-automation/` tracked folder was scanned tonight: no token/32-hex literals.
- `JUWA2_AGENT_ID` (`juwa2/client.rb`) and `VEGAS_SWEEPS_AGENT_ID` (`vegas_sweeps/client.rb`) have
  hardcoded agent-ID defaults in code. These are account identifiers, not secrets — left in place,
  no rotation needed, but their SECRET keys must only ever live in env / AgentGame.credentials.

## ON-DISK ONLY (never tracked by git — verified) — rotate if this machine is ever shared/compromised
The repo root contains local snapshots of the full production env:
`chatwoot_vars.json`, `sidekiq_vars.json`, `redis_vars.json`, `pgvector_vars.json`
(all already in .gitignore). They hold live values for everything below. Treat this
workstation as part of the secret perimeter.

| Env var name | What it is | Rotate on |
|---|---|---|
| ANTHROPIC_API_KEY | Claude API | console.anthropic.com → API keys |
| DEEPSEEK_API_KEY | DeepSeek (primary brain) | platform.deepseek.com → API keys |
| XAI_API_KEY | Grok (retired from live path) | console.x.ai |
| GEMINI_API_KEY | Google Gemini | aistudio.google.com → API keys |
| VOYAGE_API_KEY | Voyage embeddings (RAG) | dash.voyageai.com |
| CAPSOLVER_API_KEY | Captcha solving for panel logins | capsolver.com dashboard |
| ZERNIO_API_KEY | Payment provider | Zernio dashboard / support |
| FB_APP_SECRET / FB_PAGE_ACCESS_TOKEN | Facebook Graph | developers.facebook.com → app settings; regenerate page token |
| GOOGLE_OAUTH_CLIENT_SECRET | Google OAuth | console.cloud.google.com → credentials |
| SMTP_PASSWORD | Resend (email) | resend.com → API keys |
| POSTGRES_PASSWORD / PGPASSWORD / DATABASE_URL | Render Postgres | Render dashboard → database → reset password (updates DATABASE_URL) |
| REDIS_PASSWORD / REDIS_URL | Render Redis | Render dashboard → Redis |
| CHATWOOT_BRIDGE_API_TOKEN | Internal /bot bridge token (Chatwoot access token) | Patra profile settings → regenerate access token |
| SECRET_KEY_BASE | Rails session signing | generate new (`rails secret`) — NOTE: invalidates all sessions |
| ULTRA_PANDA_APP_SECRET / VBLINK_APP_SECRET / GAME_VAULT_SECRET_KEY / VEGAS_SWEEPS_SECRET_KEY / JUWA2_SECRET_KEY | Game panel secrets | each game's agent panel / upline |
| ULTRA_PANDA_AGENT_PASSWORD / VBLINK_AGENT_PASSWORD | Panel agent login passwords | change password in the panel |

## ROTATION PROCEDURE (per secret)
1. Generate/revoke on the issuing service (tables above).
2. Render dashboard → **Web Service** AND **Background Worker** → Environment → update the var.
   (Both services share most vars; missing one causes split-brain failures.)
3. Manual Deploy both services; wait for green.
4. Verify: `ruby script/patra_launch_readiness.rb` in Render Shell (section 1 = env presence,
   section 2 = live pings).
5. Update the local `*_vars.json` snapshots on this machine (or delete them).

## PRIORITY ORDER
1. TELEGRAM_BOT_TOKEN (public history exposure — confirmed)
2. JUWA_SECRET_KEY (public history exposure — confirmed)
3. Everything in the on-disk table at next convenient maintenance window.
